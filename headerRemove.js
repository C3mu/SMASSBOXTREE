var elements = document.getElementsByClassName('paper-header');

for (var i = 1; i < elements.length; i++) {
    elements[i].innerHTML = "";
}

var elements = document.getElementsByClassName('paper-watermark');

for (var i = 0; i < elements.length; i++) {
    elements[i].innerHTML = "";
}